# 0. Inicializar la base de datos interna de Superset:
docker exec -it rsn_superset superset db upgrade

# 1. Crear tu usuario administrador (te pedirá ingresar un username y password en la consola)
docker exec -it rsn_superset fab create-admin

# 1.5. 3. Crear los roles por defecto y permisos necesarios:
docker exec -it rsn_superset superset init

# 2. Migrar la base de datos interna de Superset
docker exec -it rsn_superset superset db upgrade

# 3. Inicializar roles y permisos
docker exec -it rsn_superset superset init